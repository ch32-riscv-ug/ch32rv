# PROVISIONAL overlay - rows that `ch32-device-data` does not carry yet.
# Every row here is a stopgap for a pending data request and is reported with
# `provisional: true`. Delete a row as soon as the generated DB supplies it.
# columns: sku,family,series,device_id,id_addr,flash_bytes,sram_bytes,verified,request
#
# Currently empty: nothing is outstanding. The file stays so the mechanism (and its tests)
# remain in place for the next gap - see docs/data-requests/ for how a row gets here.
